---
title: "Modal"
description: "A modal is an overlay element which blocks interaction with elements outside it."
hasSource: true
referenceUrl: "https://react-spectrum.adobe.com/react-aria/Modal.html"
hasApiReference: true
---

## Installation

<Steps>

### Initial setup

- Follow instructions on the [installation](/docs/overview/installation) page.
- See installation guides for the [`<Button />`](/docs/components/button), [`<Card />`](/docs/components/card) and [`<Dialog />`](/docs/components/dialog) components.

### Create a component

Create `modal.tsx` file.

### Source code

Copy and paste the source code into your project.

</Steps>

## Usage

<ComponentPreview slug="modal/usage" />

## Properties

### With separator

<ComponentPreview slug="modal/withSeparator" />

### Size

<ComponentPreview slug="modal/size" />

### Close button

<ComponentPreview slug="modal/closeButton" />

### Style slots

```ts
type ModalStyleSlots = "base" | "header" | "body" | "footer" | "backdrop" | "closeButton";
type ModalStyleProps = {
  classNames?: { [key in keyof ModalStyleSlots]?: string };
  styles?: { [key in keyof ModalStyleSlots]?: CSSProperties };
};
```
